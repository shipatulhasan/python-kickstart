# Process Explanation

## 1. Student-Lab-Exercises1

- Defined the variables according to the given requirements.
- Used `if/elif/else` statements to determine the appropriate access tier based on the user's input.
- Used a `while` loop to validate the developer status input.
- Required the user to enter either `y` or `n` for the developer status.
- Displayed an error message and repeatedly prompted for input if an invalid value was entered.
- Prevented the program from proceeding until a valid response (`y` or `n`) was provided.

## 2. Student-Lab-Exercises2

- Created a function to encapsulate the entire task, making it reusable for repetitive operations.
- Used a `for` loop to iterate through the `active_nodes` list nested inside the dictionary and count the active nodes.
- Calculated the cluster utilization percentage using arithmetic operations.
- Printed a clean summary report using string interpolation (f-strings).
