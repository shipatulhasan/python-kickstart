# Process Explanation

## 1. Student-Lab-Exercises1

- Defined the variables according to the given requirements.
- Used `if/elif/else` statements to determine the appropriate access tier based on the user's input.
- Used a `while` loop to validate the developer status input.
- Required the user to enter either `y` or `n` for the developer status.
- Displayed an error message and repeatedly prompted for input if an invalid value was entered.
- Prevented the program from proceeding until a valid response (`y` or `n`) was provided.

## 2. Student-Lab-Exercises2

- Created a function to encapsulate the entire task, making it reusable for repetitive operations.
- Used a `for` loop to iterate through the `active_nodes` list nested inside the dictionary and count the active nodes.
- Calculated the cluster utilization percentage using arithmetic operations.
- Printed a clean summary report using string interpolation (f-strings).

## 3. Student-Lab-Exercises3

- Created a function to calculate the estimated monthly cost of all servers.
- Converted the number of days in a month into hours and used it to calculate the total spending.
- Used `if/else` statements to return the appropriate message based on the budget status.
- Calculated the amount by which the budget was exceeded when the total spending surpassed the budget cap.

## 4. Student-Lab-Exercises4

- The main challenge was removing unwanted spaces from the list items and converting all entries to lowercase.
- Used a `for` loop to iterate through the list items.
- Applied the `strip()` method to remove leading and trailing spaces and the `lower()` method to normalize the text.
- Added each cleaned item to a new sanitized list.

## 5. Student-Lab-Exercises5

- The solution primarily relied on `if/else` statements.
- Declared a flag variable named `show_alert` and updated its value according to the given conditions.
- Used the flag variable to determine whether an alert should be displayed.
